def process_image(path, flag=False):
    if not flag:
        print("image:", path)
        return path
    else:
        # receive the image from the arduino once the flag is enabled 
        return # the image
